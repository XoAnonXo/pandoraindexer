FROM node:20-alpine

WORKDIR /app

# Копировать package.json из корня
COPY package.json package-lock.json ./
RUN npm ci --production

# Копировать скрипт
COPY scripts/recalculate-volume24h.ts ./scripts/

# Запустить из корня
CMD ["npm", "run", "recalculate:volume24h"]